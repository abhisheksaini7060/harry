import { HarryPotterMovieFiltersPipe } from './harry-potter-movie-filters.pipe';

describe('HarryPotterMovieFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new HarryPotterMovieFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
